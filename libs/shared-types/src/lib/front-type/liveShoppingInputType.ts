export interface LiveShoppingInput {
  contactId: number;
  email: string;
  firstNumber: string;
  goods_id: number;
  phoneNumber: string;
  requests: string;
  secondNumber: string;
  setDefault: boolean;
  thirdNumber: string;
  useContact: string;
}
