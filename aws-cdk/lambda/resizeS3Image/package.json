{
  "name": "resize-s3-image",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1216.0",
    "sharp": "^0.31.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.104",
    "esbuild": "0"
  },
  "scripts": {
    "build": "esbuild index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js"
  }
}
